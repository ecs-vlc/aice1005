import myutil;
size(200,0);
import myutil;
import heap;
size(250,0);
srand(3);



Heap heap = Heap();

for(int i=0; i<12; ++i) {
  heap.push(rand()%100);
}

heap.graph();
heap.list();
ship();

for(int i=0; i<5; ++i) {
  heap.graph();
  heap.list();
  highlight(i);
  tl_edge(i);
  ship();

}
